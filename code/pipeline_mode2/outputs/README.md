# Mode 2 outputs

This folder is intentionally empty in the deliverable package.

It will be populated when you run the Mode 2 pipeline. Typical artifacts include:
- `Report/Mode2Logs.csv` (feature table)
- `Report/report.html` (summary report)
- `Report/SessionReports/` (per-session HTML summaries)
